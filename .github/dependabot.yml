version: 2

updates:
  # Update npm packages
  - package-ecosystem: npm
    directory: /
    open-pull-requests-limit: 10

    # Group packages into shared PRs
    groups:
      aws-sdk:
        patterns:
          - '@aws-sdk/*'
          - 'aws*'

      core:
        patterns:
          - '@defra/*'
          - '@hapi/*'
          - 'croner'
          - 'hapi*'
          - 'joi'
          - 'mongoose'
          - 'sqs-consumer'

      build:
        patterns:
          - '@babel/*'
          - 'autoprefixer'

      lint:
        patterns:
          - 'eslint'
          - 'eslint-*'
          - 'neostandard'
          - 'prettier'
          - 'stylelint'
          - 'stylelint-*'

      logging:
        patterns:
          - '*-pino'
          - '*-pino-format'
          - 'pino'
          - 'pino-*'
          - '@elastic/*'

      testing:
        patterns:
          - '@pact-foundation/*'
          - 'vitest'
          - 'vitest-*'
          - '@vitest/*'
          - 'vitest-fetch-mock'
          - 'vitest-mongodb'

      tools:
        patterns:
          - 'convict'
          - 'convict*'
          - 'husky'
          - 'nodemon'
          - 'npm-run-all'
          - 'proxy-chain'

      types:
        patterns:
          - '@types/*'

    ignore:
      - dependency-name: 'eslint-*'
      - dependency-name: 'eslint'

    # Schedule run every Monday, local time
    schedule:
      interval: weekly
      day: monday
      time: '10:30'
      timezone: 'Europe/London'
    # Delay a PR for a package update by 5 days
    cooldown:
      default-days: 5
    versioning-strategy: increase

    allow:
      # Include direct package.json updates
      - dependency-type: direct

  # Update GitHub Actions
  - package-ecosystem: github-actions
    directory: /

    # Schedule run every Monday, local time
    schedule:
      interval: weekly
      day: monday
      time: '10:30'
      timezone: 'Europe/London'
    # Delay a PR for an action update by 5 days
    cooldown:
      default-days: 5
